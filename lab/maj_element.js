function majority_element(elems) {
	// Split array up into elements that
	if (elems.length < 2) {
		return elems;
	} 
	
	let n = Math.floor(elems.length/2); 
	
	

}

console.log(majority_element([1,5,2,3,2,2,6]))
